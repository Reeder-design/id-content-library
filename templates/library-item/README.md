# Library Item Template

The reusable item structure will be implemented in Phase 2.

Planned shape:

```text
items/<slug>/
├── item.json
├── preview/
├── source/
└── assets/
```

Not every item must use every subfolder. Folder presence will help the system determine which preview/download actions are available.
