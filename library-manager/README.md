# Library Manager

This directory will contain the lightweight local-only management utility built in Phases 5–7.

Planned responsibilities:

- browse library items
- add and edit items
- upload preview/source/assets
- infer basic metadata locally
- preview changes
- validate the library
- publish approved changes through Git

The Manager will remain local-only and should bind to `127.0.0.1` when implemented.
